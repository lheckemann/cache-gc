[package]
name = "gc"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "^1.0.136", features = ["derive"] }
serde_json = "^1.0"
chrono = "^0.4"
byte-unit = { version = "^4.0.13", default-features = false, features = ["std"] }